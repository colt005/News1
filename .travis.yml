language: android
android:
  components:
    - tools
    - android-27
    - build-tools-27.0.3
  licenses:
    - 'android-sdk-preview-license-.+'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'
before_install:
        - yes | sdkmanager "platforms;android-27.0.3"
        - chmod +x gradlew
        - touch local.properties  
branches:
        only:
                - master
